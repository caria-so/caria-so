# Flask
SECRET_KEY=change-me-in-production
SITE_URL=https://caria.so

# Set to true to show a centered logo placeholder on every route (refactor / maintenance)
MAINTENANCE_MODE=true

# Contact form → hello@caria.so (Resend SMTP example)
CONTACT_EMAIL=hello@caria.so
MAIL_FROM=hello@caria.so
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=re_your_resend_api_key
SMTP_USE_TLS=true
