# Flask
SECRET_KEY=change-me-in-production

# Contact form → hello@caria.so (Resend SMTP example)
CONTACT_EMAIL=hello@caria.so
MAIL_FROM=hello@caria.so
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=re_your_resend_api_key
SMTP_USE_TLS=true
